{% extends 'base.html.twig' %}

{% block title %}Edit SocialNetwork{% endblock %}

{% block body %}
    <div class="container" id="down">
        <div class="widget">
            <div class="col-lg-8 col-md-10 mx-auto">
    <h1 class="post-title">Edit SocialNetwork</h1>

    {{ form_start(form) }}
            <div class="input-group form-group">
            <div class="input-group-prepend">
                <span class="input-group-text"><i class="fas fa-user"></i></span>
            </div>
    {{ form_widget(form.name, { 'attr': { 'class': 'form-control' } }) }}
        </div>


        <div class="input-group form-group">
            <div class="input-group-prepend">
                <span class="input-group-text"><i class="fas fa-user"></i></span>
            </div>
    {{ form_widget(form.clientId,  {'attr': {'readonly': ''}}) }}
        </div>

    <div class="input-group form-group">
        <div class="input-group-prepend">
            <span class="input-group-text"><i class="fas fa-user"></i></span>
        </div>
    {{ form_widget(form.clientSecret, { 'attr': { 'class': 'form-control' } }) }}
    </div>



                    <a class="btn btn-outline-primary" href="{{ path('social_network_index') }}"> <span class="btn-label"><i class="fas fa-arrow-left"></i></span>
                        back to list</a>

                <button type="submit" class="btn btn-outline-success">
                <span class="btn-label"><i class="fas fa-sign-in-alt"></i></span>   {{ button_label|default('Update') }}</button>

    {{ form_end(form) }}


    {{ include('social_network/_delete_form.html.twig') }}
                </div>

        </div>
    </div>
</div>
{% endblock %}
