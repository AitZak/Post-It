{% extends 'base.html.twig' %}

{% block title %}Edit Approval{% endblock %}

{% block body %}
    <h1>Edit Approval</h1>

    {{ include('approval/_form.html.twig', {'button_label': 'Editer'}) }}

    <a href="{{ path('approval_index') }}">Retour à la liste</a>

    {{ include('approval/_delete_form.html.twig') }}
{% endblock %}
